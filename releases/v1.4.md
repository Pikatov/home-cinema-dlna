# Home Cinema v1.4 (2026‑04‑01)

## Главное
- **Security:** закрыт directory traversal в обработчиках `/video/`, `/tv/` и в DLNA `Browse` (нельзя запросить файл “выше” медиатеки через `../` и похожие трюки).
- **Bugfix:** исправлено задвоение расширения в отображаемых названиях (например, `.mkv.mkv`).

## Для разработчиков / репозиторий
- Добавлен CI на GitHub Actions (gofmt/vet/test).
- Включён Dependabot для Go‑зависимостей.
- Приведён в порядок `module` в `go.mod`.

## Обновление
- Рекомендуется обновиться всем, кто использует версии **≤ 1.3**.
- Настройки и формат данных не менялись: просто замените бинарь/приложение и перезапустите.

